import React, {useState, useEffect} from 'react';
import {
    Text,
    View,
} from 'react-native';

const HomeScreen = ({navigation}) => {

    return(
        <View>
            <Text>Working</Text>
        </View>
    )
};

export default HomeScreen;